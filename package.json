{
  "name": "david-delahaye-homepage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "build": "vite build && cp -R papers dist/papers",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
